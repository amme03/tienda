import React from 'react';
import { View, StyleSheet } from 'react-native';
import styles from './styles/item-separator';

const ItemSeparator = (
    props,
) => (
        <View style={styles.separator}/>

);






export default ItemSeparator;
